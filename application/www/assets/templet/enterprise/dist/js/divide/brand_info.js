
const brand_info = () => {
    const $document = $(document);
    const $window = $(window);
    const $target = $("#brand_info__content");
    // console.log($target.contents().find('html').height())
    // $target.contents().load(function() {
    //     console.log($target.contents().height())
    // });
    // console.log($target.contents().height())
}

export default brand_info;